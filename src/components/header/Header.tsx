import "./Header.style.scss";

const Header = () => {
	return (
		<header className={"header"}>
			<div className={"header__body"}>Github Ranks for UBS</div>
		</header>
	);
};

export default Header;
